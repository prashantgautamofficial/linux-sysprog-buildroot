# Github push and synch Commands

## Github Login

```bash
gh auth login
```

## Git init, add, remote, push

```bash
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/prashantgautamofficial/linux-sysprog-buildroot.git
git push -u origin main
```